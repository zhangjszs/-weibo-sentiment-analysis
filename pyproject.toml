# Weibo Sentiment Analysis System
# 微博舆情分析可视化系统

[project]
name = "weibo-sentiment-analysis"
version = "1.0.0"
description = "A Weibo public opinion analysis and visualization system based on Python"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "Weibo Sentiment Team"}
]

# ===== Black 代码格式化配置 =====
[tool.black]
line-length = 88
target-version = ['py311']
include = '\.pyi?$'
extend-exclude = '''
/(
    \.git
    | \.venv
    | venv
    | __pycache__
    | \.eggs
    | build
    | dist
    | \.idea
    | cache
    | logs
    | model
)/
'''

# ===== isort 导入排序配置 =====
[tool.isort]
profile = "black"
line_length = 88
multi_line_output = 3
include_trailing_comma = true
force_grid_wrap = 0
use_parentheses = true
ensure_newline_before_comments = true
skip = [".venv", "venv", "__pycache__", ".git", "build", "dist", "cache", "logs"]
known_first_party = ["config", "model", "spider", "utils", "views", "services", "tasks"]
sections = ["FUTURE", "STDLIB", "THIRDPARTY", "FIRSTPARTY", "LOCALFOLDER"]

# ===== pytest 测试配置 =====
[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--tb=short",
    "--strict-markers",
]
markers = [
    "unit: 纯逻辑，无外部服务（默认门禁）",
    "api: Flask client 测试，使用内存应用（默认门禁）",
    "integration: 需要真实数据库（SQLite 或 MySQL）",
    "external: 需要 Redis、微博 Cookie 或远程 NLP 服务",
    "slow: 性能/压力测试（每条 >2s）",
    "database: 涉及数据库层的测试",
]
# 不再一刀切忽略所有 DeprecationWarning，只忽略已知第三方 noise
filterwarnings = [
    "ignore::DeprecationWarning:celery.*",
    "ignore::DeprecationWarning:pkg_resources.*",
    "ignore::DeprecationWarning:google.*",
    "ignore::PendingDeprecationWarning:celery.*",
]

# ===== Coverage 覆盖率配置 =====
[tool.coverage.run]
source = ["src"]
omit = [
    "*/tests/*",
    "*/__pycache__/*",
    "*/venv/*",
]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
show_missing = true
fail_under = 50

# ===== MyPy 类型检查配置 (可选) =====
[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
exclude = [
    "venv",
    ".venv",
    "__pycache__",
    "build",
    "dist",
    "tests",
]

# ===== Ruff 快速代码检查配置 (可选替代 flake8) =====
[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    ".git",
    ".venv",
    "venv",
    "__pycache__",
    "build",
    "dist",
    "cache",
    "logs",
]

[tool.ruff.lint]
select = [
    "E",      # pycodestyle errors
    "W",      # pycodestyle warnings
    "F",      # pyflakes
    "I",      # isort
    "B",      # flake8-bugbear
    "C4",     # flake8-comprehensions
    "UP",     # pyupgrade
]
ignore = [
    "E501",   # line too long (handled by black)
    "B008",   # do not perform function calls in argument defaults - 现有代码大量使用此模式，重构风险高
    "B905",   # zip without explicit strict - 现有代码大量使用zip()，添加strict=True可能引入行为变化
]
