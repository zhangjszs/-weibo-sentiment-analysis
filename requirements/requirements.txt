# ===========================================
# 微博舆情分析系统 - 后端依赖（SOTA 升级版）
# Python >= 3.11
# ===========================================
# torch 安装 CPU 版本（避免拉 GPU 大包）：
#   pip install torch --extra-index-url https://download.pytorch.org/whl/cpu
# 完整安装：
#   pip install -r requirements/requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# ===========================================

# 基础依赖
jieba==0.42.1
snownlp==0.12.3
pandas~=2.2.3
numpy~=2.2.3
scikit-learn~=1.6.1
PyMySQL==1.1.1
matplotlib>=3.8.0
wordcloud==1.9.4
pillow>=10.0.0

# Web 框架（Flask 3.1 / SQLAlchemy 2.0）
Flask~=3.1.0
SQLAlchemy~=2.0.40
requests~=2.32.0
Flask-DebugToolbar>=0.11.1
# werkzeug / urllib3 不再显式钉死，随 Flask / requests 自行解析

# 异步任务队列（Celery 5.3 / Redis 5.0）
celery[redis]~=5.3.6
redis~=5.0.1

# 数据校验和熔断器
pydantic~=2.5.3
circuitbreaker==1.3.2

# 布隆过滤器去重
pybloom-live==4.0.0

# 其他工具
python-dotenv==1.0.0
prometheus-client==0.19.0
flask-cors>=4.0.0
gunicorn==22.0.0
gevent==23.9.1
flask-socketio>=5.3.6
python-socketio>=5.9.0
python-engineio>=4.8.0
PyJWT>=2.0

# 安全与密码
bcrypt>=4.0
cryptography>=40.0

# Flask 扩展
flask-compress>=1.13
flask-wtf>=1.1.0
brotli>=1.0             # Brotli 压缩算法支持

# 报告生成
reportlab>=3.6
python-pptx>=0.6

# 数据科学与可视化
joblib>=1.0
seaborn>=0.12
emoji>=2.0

# BERT 情感分析（Phase 3）
transformers~=4.44
torch>=2.2,<3.0
onnxruntime>=1.17
optimum[onnxruntime]>=1.17
sentencepiece>=0.2
huggingface_hub>=0.20

# 其他工具
jsonpath>=0.82
# 构建工具不钉死版本，由 pip 自行解析
# wheel / setuptools 不再显式约束（旧版本 wheel==0.46.3 是无效版本号）
